# ── Chain ─────────────────────────────────────────────────────────────────────
# Celo mainnet RPC (defaults to https://forno.celo.org if unset)
CELO_RPC_URL=https://forno.celo.org
CHAIN_ID=42220

# ── Agent wallet (deployer key = agent identity) ───────────────────────────────
# Private key of the wallet that deploys contracts AND acts as the AI agent.
# Used to: sign ERC-8004 Self Agent ID requests, broadcast agent-initiated txs.
AGENT_PRIVATE_KEY=0x_your_deployer_private_key

# Self Agent ID — populated by `npm run register:agent` after passport scan.
# Format: numeric NFT token ID e.g. 42
# After linked-mode registration, replace AGENT_PRIVATE_KEY with the exported agent key from the script.
AGENT_SELF_ID=

# 8004scan ERC-8004 Identity Registry — populated by `npm run register:8004scan`
AGENT_8004_ID=
# Optional overrides for register-8004scan.ts
# AGENT_8004_NAME=Cowry AI Agent
# AGENT_8004_DESCRIPTION=Conversational crypto payments on Celo...
# AGENT_8004_UPDATE=1   # or: npm run register:8004scan -- --update
# AGENT_API_URL=https://your-public-api.example.com
# AGENT_8004_URI=https://... or ipfs://... (default: embedded data URI)

# ── Deployed contract addresses ───────────────────────────────────────────────
# Copy from smartcontract/deployments/celo-mainnet.json after deploying.
USERNAME_REGISTRY_ADDRESS=0x3b89d7b4997db5645db2829523ed3e79e55a0f02
GROUP_REGISTRY_ADDRESS=0xee7ee3852663917a12ef95852a9fc4e092e45a31
COWRYPAY_ADDRESS=0x2b0d2f1dec9ab3e06668145d21ed17715e288350

# ── AI ────────────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
# Groq — fast/cheap, default model for intent parsing, institution matching, and chat replies.
GROQ_API_KEY=gsk_...
# Anthropic (Claude) — optional fallback used only when Groq can't confidently
# resolve an institution name or is unavailable. Without this set, those calls
# just skip straight to the existing fallback (full institution list / generic reply).
ANTHROPIC_API_KEY=sk-ant-...

# ── Privy (server-side wallet creation for WhatsApp flow — optional) ──────────
PRIVY_APP_ID=
PRIVY_APP_SECRET=

# ── LI.FI (cross-chain bridge + Earn vaults) ─────────────────────────────────
# Optional — without it requests still work but are rate-limited
LIFI_API_KEY=
# Shown in LI.FI analytics dashboard (identifies your integration)
LIFI_INTEGRATOR=sendpay

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3001
